🚀 MASTER PROMPT FOR AI AGENT: GRSS FIELD ANALYST V2.1 (STABILITY & GOD-MODE)
Context: You are an expert Principal Full-Stack Engineer working on the "GRSS Field Analyst" live-event gamified platform. The application uses Next.js, Socket.io, Zustand, and MongoDB. It must flawlessly support 250 concurrent users.

Objective: Implement a critical suite of backend resilience upgrades, "God Mode" admin controls, clock-skew mitigation, and anti-cheat measures based on the V2.1 architectural review.

Execution Rules:

Do not remove or break existing functionality (especially the dynamic question bank and image preloading).

Execute the implementation step-by-step, modifying the specific files requested.

Ensure all new Socket.io events are properly typed in realtime/game/types.ts.

Please process and implement the following phases exactly as specified:

🛠️ PHASE 1: GAME ENGINE & TYPES CORE (Backend)
Files to modify: realtime/game/types.ts, realtime/game/GameEngine.ts

Clock Skew Types: In types.ts, update TimerStartPayload and GameStateSync to include serverTime: number.

Live Stats Types: Add an AdminLiveStatsPayload type to track real-time answer distribution (e.g., Record<string, number> for A/B/C/D or text answers).

Engine Clock Skew: In GameEngine.ts, whenever you emit timer_start or game_state_sync, attach serverTime: Date.now() to the payload.

Engine Force End: In GameEngine.ts, change private endQuestion() to public forceEndQuestion() (or add a public wrapper) so admins can trigger it manually.

Engine Player Kick: In GameEngine.ts, add a public kickPlayer(usn: string) method that removes the user from playerScores, connectedPlayers, and forces a disconnect on their socket.

Engine Memory Leak Fix: In GameEngine.ts inside hydrateFromDb(), explicitly call this.clearTimer() right before this.startCountdown(this.timerRemaining, ...) to kill any zombie intervals during PM2 restarts.

Engine Live Stats: Add a method to calculate current answer distributions for the active question and store it, ready to emit to admins.

🛡️ PHASE 2: SOCKET LAYER & DB RESILIENCE
Files to modify: realtime/sockets/game.ts

MongoDB Connection Guard: Locate the setInterval block running the DB bulkWrites at the bottom of the file. Wrap the User.bulkWrite call in an if (mongoose.connection.readyState === 1) check to prevent query build-ups if the DB temporarily disconnects.

Hangman Rate Limiting: In the socket.on('guess_letter') listener, implement a 150ms rate limit using the existing rateLimitMap (or a dedicated one) to prevent malicious scripts from blasting the server with alphabet arrays.

Admin Listeners: Add listeners for:

admin_force_end_question: Calls engine.forceEndQuestion().

admin_kick_player: Calls engine.kickPlayer(usn), deletes the user via User.deleteOne({ usn }), and emits a targeted force_disconnect to that user's socket ID.

Admin Emit Live Stats: In the submit_answer listener, after a user submits, emit an updated live answer distribution stats payload exclusively to connected admins.

🔄 PHASE 3: ZUSTAND STATE MANAGEMENT
Files to modify: stores/useGameSyncStore.ts

Clock Skew Offset: * Add serverTimeOffset: number to the state (default 0).

In the timer_start and game_state_sync socket listeners, calculate and store the offset: offset = Date.now() - data.serverTime.

Auction Toast Fix: In the auction_update listener, if data.success is false and data.error exists, import and call toast(data.error, 'err') so users know why a purchase failed (e.g., someone else bought it first triggering a price hike).

Admin Actions: Add adminForceEndQuestion, adminKickPlayer(usn: string) to the store actions and emit them via the socket.

Live Stats State: Add an adminLiveStats object to the store to catch the real-time answer distribution broadcasts.

🎨 PHASE 4: FRONTEND PLAYER UX
Files to modify: components/game/GameHUD.tsx, components/game/LevelIntroPhase.tsx, components/game/QuestionPhase.tsx

Perfect Timer Sync: In GameHUD.tsx and LevelIntroPhase.tsx, update the requestAnimationFrame loop to utilize the offset. Calculate remaining time as: Math.max(0, timerEndTime - (Date.now() - serverTimeOffset)).

Image CLS Fix: In QuestionPhase.tsx, locate the ImageMCQ component. Update the <img src={q.imageUrl} /> tag to include a specific aspect-ratio or minHeight (e.g., minHeight: '250px'), and apply a CSS skeleton loading state (e.g., className="skeleton") while the image resolves, preventing layout shift.

👑 PHASE 5: GOD-MODE ADMIN PANEL
Files to modify: app/admin/page.tsx

Force End Button: In the "Game Control" section, add a prominent "⚠️ FORCE END QUESTION" button (disabled unless phase === 'question_active'). Hook it up to adminForceEndQuestion.

Live Clairvoyance Panel: Create a new card titled "👁️ LIVE ANSWER DISTRIBUTION" (visible only during question_active). Render the data from adminLiveStats showing exactly what players are guessing in real-time.

Player Roster & Kick: In the "LIVE LEADERS" section (or a new "ROSTER" section), add a small "🚫 KICK" button next to each player's name. When clicked, confirm via window.confirm, then trigger adminKickPlayer(usn).

🚀 PHASE 6: DEPLOYMENT DOCUMENTATION
Files to modify: README.md

Add a "CRITICAL DEPLOYMENT ARCHITECTURE" section to the README.

Explicitly document that the frontend/API must be deployed to a Serverless provider (Vercel/Netlify), while the Realtime Server (realtime/server.ts) MUST be deployed to a long-running stateful instance (Render/Railway/AWS).

Note that NEXT_PUBLIC_SOCKET_URL must be set in the frontend environment variables to point to the dedicated socket server.